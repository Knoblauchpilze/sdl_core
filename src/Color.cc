
# include "Color.hh"

namespace sdl {
  namespace core {

    const Color Color::White(255, 255, 255, SDL_ALPHA_OPAQUE);
    const Color Color::Black(0, 0, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Red(255, 0, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Green(0, 255, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Blue(0, 0, 255, SDL_ALPHA_OPAQUE);
    const Color Color::Yellow(255, 255, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Orange(255, 128, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Cyan(0, 255, 255, SDL_ALPHA_OPAQUE);
    const Color Color::Magenta(255, 0, 255, SDL_ALPHA_OPAQUE);
    const Color Color::Silver(192, 192, 192, SDL_ALPHA_OPAQUE);
    const Color Color::Gray(128, 128, 128, SDL_ALPHA_OPAQUE);
    const Color Color::Maroon(128, 0, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Olive(128, 128, 0, SDL_ALPHA_OPAQUE);
    const Color Color::Purple(128, 0, 128, SDL_ALPHA_OPAQUE);
    const Color Color::Teal(0, 128, 128, SDL_ALPHA_OPAQUE);
    const Color Color::Navy(0, 0, 128, SDL_ALPHA_OPAQUE);
    const Color Color::CorneFlowerBlue(100, 149, 237, SDL_ALPHA_OPAQUE);

  }
}
